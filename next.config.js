/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
}

module.exports = {
  assetPrefix: './',
  basePath: '/portifolio',
  trailingSlash: true,
}

module.exports = nextConfig